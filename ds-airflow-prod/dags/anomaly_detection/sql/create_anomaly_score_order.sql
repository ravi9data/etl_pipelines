CREATE TABLE IF NOT EXISTS data_science_dev.anomaly_score_order (order_id VARCHAR(128),
                                                                 anomaly_score float,
                                                                 created_at TIMESTAMP);
